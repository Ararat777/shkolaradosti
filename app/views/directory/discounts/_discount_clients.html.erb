<table class="table">
    <thead>
      <tr>
        <th scope="col">Клиент</th>
        <th scope="col">Филиал</th>
        <th scope="col">Действия</th>
      </tr>
    </thead>
    <tbody>
      <% discount.clients.each do |item| %>
      <tr>
        <td><%= item.name %></td>
        <td><%= item.branch.title %></td>
        <td><%= link_to "Удалить",remove_discount_client_directory_discount_path(discount.id,item.id),method: :delete %></td>
      </tr>
      <% end %>
    </tbody>
</table>